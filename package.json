{
  "name": "blogify-static",
  "version": "0.1.0",
  "private": true,
  "description": "A static blog site generated from a Next.js base.",
  "scripts": {
    "serve": "live-server",
    "start": "npm run serve",
    "build": "echo 'No build step needed for basic static site.'"
  },
  "devDependencies": {
     "live-server": "^1.2.2"
  }
}
